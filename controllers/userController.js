import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

const addUser = async (req, res) => { return;};

const UserController = { addUser };

export default UserController;